spring:
  datasource:
    username: root
    password: 123456
    url: jdbc:mysql://192.168.56.10:3306/gulimall_wms
    driver-class-name: com.mysql.cj.jdbc.Driver
  application:
    name: xmall-ware
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8838
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
  main:
    allow-circular-references: true
mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
server:
  port: 12000
logging:
  level:
    com.example: debug

